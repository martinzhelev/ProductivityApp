<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Social - Amblezio</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/amblezio.css">
    <link rel="stylesheet" href="/css/social.css">
</head>
<body class="bg-dark text-light">
    <!-- Animated Background -->
    <div class="animated-bg"></div>
    
    <!-- Modern Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top glass-nav">
        <div class="container">
            <a class="navbar-brand fw-bold fs-3" href="/home/<%= userId %>">
                <i class="fas fa-rocket me-2 text-primary"></i>
                <span class="gradient-text">Amblezio</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/profile/<%= userId %>">
                            <i class="fas fa-user me-1"></i>Profile
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/subscribe/<%= userId %>">
                            <i class="fas fa-credit-card me-1"></i>Premium
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">
                            <i class="fas fa-sign-out-alt me-1"></i>Logout
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Offcanvas Menu -->
    <div class="offcanvas offcanvas-end glass-nav" tabindex="-1" id="offcanvasNavbar">
        <div class="offcanvas-header">
            <h5 class="offcanvas-title gradient-text">Navigation</h5>
            <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas"></button>
        </div>
        <div class="offcanvas-body">
            <ul class="nav flex-column">
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/home/<%= userId %>">
                        <i class="fas fa-home me-2"></i>Dashboard
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/subscribe/<%= userId %>">
                        <i class="fas fa-credit-card me-2"></i>Payments
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/profile/<%= userId %>">
                        <i class="fas fa-user me-2"></i>Profile
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/work/<%= userId %>">
                        <i class="fas fa-briefcase me-2"></i>Work
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/mental/<%= userId %>">
                        <i class="fas fa-brain me-2"></i>Mental
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/calorietracker/<%= userId %>">
                        <i class="fas fa-utensils me-2"></i>Calorie Tracker
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link active" href="/social/<%= userId %>">
                        <i class="fas fa-users me-2"></i>Social
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/timeoff/<%= userId %>">
                        <i class="fas fa-clock me-2"></i>Time Off
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/body/<%= userId %>">
                        <i class="fas fa-dumbbell me-2"></i>Body
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link modern-nav-link logout-link" href="/logout">
                        <i class="fas fa-sign-out-alt me-2"></i>Logout
                    </a>
                </li>
            </ul>
        </div>
    </div>

    <!-- Main Content -->
    <main class="container-fluid px-4 py-5" style="margin-top: 80px;">
        <!-- Page Header -->
        <div class="row mb-5">
            <div class="col-12">
                <div class="page-header-card glass text-center py-4">
                    <div class="page-icon mb-3">
                        <i class="fas fa-users"></i>
                    </div>
                    <h1 class="page-title mb-3">
                        <span class="gradient-text">Social Hub</span>
                    </h1>
                    <p class="page-subtitle">Connect with people and track your social events</p>
                </div>
            </div>
        </div>

        <div class="row g-4">
            <!-- Social Events Section -->
            <div class="col-lg-6">
                <!-- Add Event Card -->
                <div class="modern-card glass mb-4">
                    <div class="card-header-modern">
                        <div class="d-flex align-items-center">
                            <div class="card-icon event-icon">
                                <i class="fas fa-plus-circle"></i>
                            </div>
                            <h3 class="card-title-modern mb-0">Add Social Event</h3>
                        </div>
                    </div>
                    <div class="card-body-modern">
                        <form id="eventForm">
                            <div class="form-group-modern mb-3">
                                <label for="eventName" class="form-label-modern">
                                    <i class="fas fa-calendar-plus me-2"></i>Event Name
                                </label>
                                <input type="text" id="eventName" class="form-control-modern" 
                                    placeholder="Enter event name" required>
                            </div>
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <div class="form-group-modern">
                                        <label for="eventDate" class="form-label-modern">
                                            <i class="fas fa-calendar me-2"></i>Date
                                        </label>
                                        <input type="date" id="eventDate" class="form-control-modern" required>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group-modern">
                                        <label for="eventTime" class="form-label-modern">
                                            <i class="fas fa-clock me-2"></i>Time
                                        </label>
                                        <input type="time" id="eventTime" class="form-control-modern" required>
                                    </div>
                                </div>
                            </div>
                            <button id="saveEventBtn" class="btn btn-primary w-100 mt-3 pulse-btn">
                                <i class="fas fa-save me-2"></i>Save Event
                            </button>
                        </form>
                    </div>
                </div>

                <!-- Events List Card -->
                <div class="modern-card glass">
                    <div class="card-header-modern">
                        <div class="d-flex align-items-center">
                            <div class="card-icon events-icon">
                                <i class="fas fa-calendar-check"></i>
                            </div>
                            <h3 class="card-title-modern mb-0">Social Events</h3>
                        </div>
                    </div>
                    <div class="card-body-modern">
                        <div class="events-container">
                            <div id="eventList">
                                <% if (socializingData.length > 0) { %>
                                    <% socializingData.forEach(event => { %>
                                        <div class="event-item-modern">
                                            <div class="event-main">
                                                <div class="event-info">
                                                    <h5 class="event-name">
                                                        <i class="fas fa-calendar-alt me-2"></i>
                                                        <%= event.event_name || 'Social Event' %>
                                                    </h5>
                                                    <div class="event-details">
                                                        <span class="event-date">
                                                            <i class="fas fa-calendar me-1"></i>
                                                            <%= new Date(event.date).toLocaleDateString('en-GB') %>
                                                        </span>
                                                        <span class="event-time">
                                                            <i class="fas fa-clock me-1"></i>
                                                            <%= event.time %>
                                                        </span>
                                                    </div>
                                                </div>
                                                <div class="event-actions">
                                                    <button class="btn btn-success btn-sm complete-event pulse-btn" data-id="<%= event.id %>">
                                                        <i class="fas fa-check"></i>
                                                    </button>
                                                    <button class="btn btn-outline-danger btn-sm delete-event" data-id="<%= event.id %>">
                                                        <i class="fas fa-trash"></i>
                                                    </button>
                                                </div>
                                            </div>
                                        </div>
                                    <% }) %>
                                <% } else { %>
                                    <div class="empty-state">
                                        <i class="fas fa-calendar-plus"></i>
                                        <h4>No social events yet</h4>
                                        <p>Start building your social calendar by adding your first event!</p>
                                    </div>
                                <% } %>
                            </div>
                        </div>
                        
                        <!-- Event Pagination -->
                        <% if (totalEventPages > 1) { %>
                            <div class="pagination-modern mt-4">
                                <button id="prevEventPage" class="page-link-modern" <%= eventPage === 1 ? 'disabled' : '' %>>
                                    <i class="fas fa-chevron-left"></i>
                                    <span>Previous</span>
                                </button>
                                <span class="page-info">
                                    Page <%= eventPage %> of <%= totalEventPages %>
                                </span>
                                <button id="nextEventPage" class="page-link-modern" <%= eventPage === totalEventPages ? 'disabled' : '' %>>
                                    <span>Next</span>
                                    <i class="fas fa-chevron-right"></i>
                                </button>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>

            <!-- People Section -->
            <div class="col-lg-6">
                <!-- Add Person Card -->
                <div class="modern-card glass mb-4">
                    <div class="card-header-modern">
                        <div class="d-flex align-items-center">
                            <div class="card-icon person-icon">
                                <i class="fas fa-user-plus"></i>
                            </div>
                            <h3 class="card-title-modern mb-0">Add Person</h3>
                        </div>
                    </div>
                    <div class="card-body-modern">
                        <form id="personForm">
                            <div class="form-group-modern mb-3">
                                <label for="personName" class="form-label-modern">
                                    <i class="fas fa-user me-2"></i>Name
                                </label>
                                <input type="text" id="personName" class="form-control-modern" 
                                    placeholder="Enter name" required>
                            </div>
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <div class="form-group-modern">
                                        <label for="personBirthday" class="form-label-modern">
                                            <i class="fas fa-birthday-cake me-2"></i>Birthday
                                        </label>
                                        <input type="date" id="personBirthday" class="form-control-modern">
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group-modern">
                                        <label for="personGift" class="form-label-modern">
                                            <i class="fas fa-gift me-2"></i>Gift Idea
                                        </label>
                                        <input type="text" id="personGift" class="form-control-modern" 
                                            placeholder="Gift idea">
                                    </div>
                                </div>
                            </div>
                            <button id="savePersonBtn" class="btn btn-primary w-100 mt-3 pulse-btn">
                                <i class="fas fa-save me-2"></i>Save Person
                            </button>
                        </form>
                    </div>
                </div>

                <!-- People List Card -->
                <div class="modern-card glass">
                    <div class="card-header-modern">
                        <div class="d-flex align-items-center">
                            <div class="card-icon people-icon">
                                <i class="fas fa-users"></i>
                            </div>
                            <h3 class="card-title-modern mb-0">People</h3>
                        </div>
                    </div>
                    <div class="card-body-modern">
                        <div class="people-container">
                            <div id="peopleList">
                                <% if (peopleData && peopleData.length > 0) { %>
                                    <% peopleData.forEach(person => { %>
                                        <div class="person-item-modern" data-id="<%= person.id %>">
                                            <div class="person-header">
                                                <div class="person-info">
                                                    <h5 class="person-name editable" data-field="name">
                                                        <i class="fas fa-user me-2"></i>
                                                        <%= person.name || 'Unknown' %>
                                                    </h5>
                                                    <div class="person-birthday">
                                                        <i class="fas fa-birthday-cake me-1"></i>
                                                        <span class="editable" data-field="birthday">
                                                            <%= person.birthday ? new Date(person.birthday).toLocaleDateString('en-GB') : 'Not set' %>
                                                        </span>
                                                    </div>
                                                </div>
                                                <button class="btn btn-outline-danger btn-sm delete-person" data-id="<%= person.id %>">
                                                    <i class="fas fa-trash"></i>
                                                </button>
                                            </div>
                                            
                                            <div class="gifts-section">
                                                <h6 class="gifts-title">
                                                    <i class="fas fa-gift me-2"></i>Gift Ideas
                                                </h6>
                                                <div class="gifts-list">
                                                    <% if (person.gifts && person.gifts.length > 0) { %>
                                                        <% person.gifts.forEach(gift => { %>
                                                            <div class="gift-item-modern">
                                                                <div class="gift-info">
                                                                    <span class="gift-name"><%= gift.gift_name %></span>
                                                                    <small class="gift-date">
                                                                        Added: <%= new Date(gift.added_date).toLocaleDateString('en-GB') %>
                                                                    </small>
                                                                </div>
                                                                <button class="btn btn-outline-danger btn-sm delete-gift" data-gift-id="<%= gift.id %>">
                                                                    <i class="fas fa-times"></i>
                                                                </button>
                                                            </div>
                                                        <% }) %>
                                                    <% } else { %>
                                                        <div class="no-gifts">
                                                            <i class="fas fa-gift"></i>
                                                            <span>No gifts yet</span>
                                                        </div>
                                                    <% } %>
                                                </div>
                                                
                                                <div class="add-gift-section">
                                                    <div class="input-group-modern">
                                                        <input type="text" class="form-control-modern add-gift-input" 
                                                            placeholder="Add gift idea" data-person-id="<%= person.id %>">
                                                        <button class="btn btn-primary add-gift-btn pulse-btn">
                                                            <i class="fas fa-plus"></i>
                                                        </button>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    <% }) %>
                                <% } else { %>
                                    <div class="empty-state">
                                        <i class="fas fa-user-plus"></i>
                                        <h4>No people added yet</h4>
                                        <p>Start building your network by adding people you know!</p>
                                    </div>
                                <% } %>
                            </div>
                        </div>
                        
                        <!-- People Pagination -->
                        <% if (totalPeoplePages > 1) { %>
                            <div class="pagination-modern mt-4">
                                <button id="prevPeoplePage" class="page-link-modern" <%= peoplePage === 1 ? 'disabled' : '' %>>
                                    <i class="fas fa-chevron-left"></i>
                                    <span>Previous</span>
                                </button>
                                <span class="page-info">
                                    Page <%= peoplePage %> of <%= totalPeoplePages %>
                                </span>
                                <button id="nextPeoplePage" class="page-link-modern" <%= peoplePage === totalPeoplePages ? 'disabled' : '' %>>
                                    <span>Next</span>
                                    <i class="fas fa-chevron-right"></i>
                                </button>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script>
        window.userId = '<%= userId %>';
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/frontendJS/socialFE.js"></script>
</body>
</html>