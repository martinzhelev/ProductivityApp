<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Work Management - Amblezio</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/amblezio.css">
    <link rel="stylesheet" href="/css/work.css">
</head>
<body class="bg-dark text-light">
    <!-- Animated Background -->
    <div class="animated-bg"></div>
    
    <!-- Modern Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top glass-nav">
        <div class="container">
            <a class="navbar-brand fw-bold fs-3" href="/home/<%= userId %>">
                <i class="fas fa-rocket me-2 text-primary"></i>
                <span class="gradient-text">Amblezio</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/profile/<%= userId %>">
                            <i class="fas fa-user me-1"></i>Profile
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/subscribe/<%= userId %>">
                            <i class="fas fa-credit-card me-1"></i>Premium
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">
                            <i class="fas fa-sign-out-alt me-1"></i>Logout
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Offcanvas Menu -->
    <div class="offcanvas offcanvas-end glass-nav" tabindex="-1" id="offcanvasNavbar">
        <div class="offcanvas-header">
            <h5 class="offcanvas-title gradient-text">Navigation</h5>
            <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas"></button>
        </div>
        <div class="offcanvas-body">
            <ul class="nav flex-column">
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/home/<%= userId %>">
                        <i class="fas fa-home me-2"></i>Dashboard
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/subscribe/<%= userId %>">
                        <i class="fas fa-credit-card me-2"></i>Payments
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/profile/<%= userId %>">
                        <i class="fas fa-user me-2"></i>Profile
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link active" href="/work/<%= userId %>">
                        <i class="fas fa-briefcase me-2"></i>Work
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/mental/<%= userId %>">
                        <i class="fas fa-brain me-2"></i>Mental
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/calorietracker/<%= userId %>">
                        <i class="fas fa-utensils me-2"></i>Calorie Tracker
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/social/<%= userId %>">
                        <i class="fas fa-users me-2"></i>Social
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/timeoff/<%= userId %>">
                        <i class="fas fa-clock me-2"></i>Time Off
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link modern-nav-link" href="/body/<%= userId %>">
                        <i class="fas fa-dumbbell me-2"></i>Body
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link modern-nav-link logout-link" href="/logout">
                        <i class="fas fa-sign-out-alt me-2"></i>Logout
                    </a>
                </li>
            </ul>
        </div>
    </div>

    <!-- Main Content -->
    <main class="container-fluid px-4 py-5" style="margin-top: 80px;">
        <!-- Page Header -->
        <div class="row mb-5">
            <div class="col-12">
                <div class="page-header-card glass text-center py-4">
                    <div class="page-icon mb-3">
                        <i class="fas fa-briefcase"></i>
                    </div>
                    <h1 class="page-title mb-3">
                        <span class="gradient-text">Work Management</span>
                    </h1>
                    <p class="page-subtitle">Organize your deadlines and tasks efficiently</p>
                </div>
            </div>
                    </div>

        <div class="row g-4">
            <!-- Deadlines Section -->
            <div class="col-lg-6">
                <!-- Add Deadline Card -->
                <div class="modern-card glass mb-4">
                    <div class="card-header-modern">
                        <div class="d-flex align-items-center">
                            <div class="card-icon deadline-icon">
                                <i class="fas fa-plus-circle"></i>
                        </div>
                            <h3 class="card-title-modern mb-0">Add Deadline</h3>
                        </div>
                    </div>
                    <div class="card-body-modern">
                        <form id="deadlineForm">
                            <div class="form-group-modern mb-3">
                                <label for="deadlineName" class="form-label-modern">
                                    <i class="fas fa-calendar-plus me-2"></i>Deadline Name
                                </label>
                                <input type="text" id="deadlineName" class="form-control-modern" 
                                    placeholder="Enter deadline name" required>
                            </div>
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <div class="form-group-modern">
                                        <label for="deadlineDate" class="form-label-modern">
                                            <i class="fas fa-calendar me-2"></i>Date
                                        </label>
                                        <input type="date" id="deadlineDate" class="form-control-modern" required>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group-modern">
                                        <label for="deadlineTime" class="form-label-modern">
                                            <i class="fas fa-clock me-2"></i>Time
                                        </label>
                                        <input type="time" id="deadlineTime" class="form-control-modern" required>
                                    </div>
                                </div>
                            </div>
                            <button id="saveDeadlineBtn" class="btn btn-primary w-100 mt-3 pulse-btn">
                                <i class="fas fa-save me-2"></i>Save Deadline
                            </button>
                        </form>
                    </div>
                </div>

                <!-- Deadlines List Card -->
                <div class="modern-card glass">
                    <div class="card-header-modern">
                        <div class="d-flex align-items-center">
                            <div class="card-icon deadlines-icon">
                                <i class="fas fa-calendar-check"></i>
                            </div>
                            <h3 class="card-title-modern mb-0">Deadlines</h3>
                        </div>
                    </div>
                    <div class="card-body-modern">
                        <div class="deadlines-container">
                            <div id="deadlineList">
                        <% if (deadlines.length > 0) { %>
                            <% deadlines.forEach(deadline => { %>
                                        <div class="deadline-item-modern">
                                            <div class="deadline-main">
                                                <div class="deadline-info">
                                                    <h5 class="deadline-name">
                                                        <i class="fas fa-calendar-alt me-2"></i>
                                                        <%= deadline.deadline %>
                                                    </h5>
                                                    <div class="deadline-details">
                                                        <span class="deadline-date">
                                                            <i class="fas fa-calendar me-1"></i>
                                                            <%= new Date(deadline.date).toLocaleDateString('en-GB') %>
                                                        </span>
                                                        <span class="deadline-time">
                                                            <i class="fas fa-clock me-1"></i>
                                                            <%= new Date(deadline.date).toLocaleTimeString('en-GB', { hour: '2-digit', minute: '2-digit' }) %>
                                                        </span>
                                                    </div>
                                                </div>
                                                <div class="deadline-actions">
                                                    <button class="btn btn-outline-danger btn-sm delete-deadline" data-id="<%= deadline.deadline_id %>">
                                                        <i class="fas fa-trash"></i>
                                                    </button>
                                                </div>
                                    </div>
                                </div>
                            <% }) %>
                        <% } else { %>
                                    <div class="empty-state">
                                        <i class="fas fa-calendar-plus"></i>
                                        <h4>No deadlines yet</h4>
                                        <p>Start organizing your work by adding your first deadline!</p>
                                    </div>
                        <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Tasks Section -->
            <div class="col-lg-6">
                <div class="modern-card glass">
                    <div class="card-header-modern">
                        <div class="d-flex align-items-center">
                            <div class="card-icon tasks-icon">
                                <i class="fas fa-tasks"></i>
                            </div>
                            <h3 class="card-title-modern mb-0">Tasks</h3>
                        </div>
                    </div>
                    <div class="card-body-modern">
                        <div class="tasks-container">
                            <div id="tasks-list">
                        <% if (tasks.length > 0) { %>
                            <% tasks.forEach(task => { %>
                                        <div class="task-item-modern" data-task-id="<%= task.task_id %>">
                                            <div class="task-main">
                                                <div class="task-checkbox">
                                                    <input type="checkbox" class="modern-checkbox checkbox" <%= task.completed ? 'checked' : '' %>>
                                                    <div class="checkmark"></div>
                                                </div>
                                                <div class="task-content">
                                                    <span class="task-text <%= task.completed ? 'completed' : '' %>">
                                        <%= task.task %>
                                    </span>
                                                </div>
                                                <div class="task-actions">
                                                    <button class="btn btn-outline-danger btn-sm delete-task" data-id="<%= task.task_id %>">
                                                        <i class="fas fa-trash"></i>
                                                    </button>
                                                </div>
                                            </div>
                                        </div>
                                    <% }) %>
                        <% } else { %>
                                    <div class="empty-state">
                                        <i class="fas fa-tasks"></i>
                                        <h4>No tasks yet</h4>
                                        <p>Start organizing your work by adding your first task!</p>
                                    </div>
                        <% } %>
                            </div>
                        </div>

                        <div class="task-actions-modern mt-4">
                            <button class="btn btn-primary w-100 mb-2 pulse-btn" id="addTask">
                                <i class="fas fa-plus me-2"></i>Add Task
                            </button>
                            <button class="btn btn-outline-danger w-100 pulse-btn" id="removeDoneTask">
                                <i class="fas fa-trash me-2"></i>Remove Done Tasks
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

        <!-- Add Task Modal -->
        <div class="modal fade" id="taskModal" tabindex="-1" aria-labelledby="taskModalLabel" aria-hidden="true">
            <div class="modal-dialog">
            <div class="modal-content modern-modal">
                <div class="modal-header modern-modal-header">
                    <h5 class="modal-title" id="taskModalLabel">
                        <i class="fas fa-plus me-2"></i>Add New Task
                    </h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                <div class="modal-body modern-modal-body">
                    <div class="form-group-modern">
                        <label for="taskName" class="form-label-modern">
                            <i class="fas fa-tasks me-2"></i>Task Name
                        </label>
                        <input type="text" id="taskName" class="form-control-modern" 
                            placeholder="Enter task name">
                    </div>
                </div>
                <div class="modal-footer modern-modal-footer">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times me-1"></i>Close
                    </button>
                    <button type="button" id="saveTaskBtn" class="btn btn-primary pulse-btn">
                        <i class="fas fa-check me-1"></i>Add Task
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/frontendJS/workFE.js"></script>
</body>
</html>
