<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Payments</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"/>
  <link rel="stylesheet" href="/css/main.css"/>
</head>
<body class="bg-light">

  <!-- Header -->
  <header class="bg-dark text-white p-3">
    <div class="container d-flex justify-content-between align-items-center">
      <div class="profile d-flex align-items-center">
        <span class="ms-2">
          <%= username %>
        </span>
      </div>
      <button class="btn btn-outline-light" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar">
        ☰
      </button>
    </div>
  </header>

  <!-- Offcanvas Menu -->
  <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasNavbar">
    <div class="offcanvas-header">
      <h5 class="offcanvas-title">Menu</h5>
      <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas"></button>
    </div>
    <div class="offcanvas-body">
      <ul class="nav flex-column">
        <li class="nav-item">
          <a class="nav-link" href="/home/<%= userId %>">🏠 Dashboard</a>
      </li>
      <li class="nav-item">
          <a class="nav-link" href="/subscribe/<%= userId %>">💳 Payments</a>
      </li>
      <li class="nav-item">
          <a class="nav-link" href="/profile/<%= userId %>">👤 Profile</a>
      </li>
      <li class="nav-item">
          <a class="nav-link" href="/work/<%= userId %>">💼 Work</a>
      </li>
      <li class="nav-item">
          <a class="nav-link" href="/mental/<%= userId %>">🧠 Mental</a>
      </li>
      <li class="nav-item">
          <a class="nav-link" href="/calorietracker/<%= userId %>">🍽️ Calorie Tracker</a>
      </li>
      <li class="nav-item">
          <a class="nav-link" href="/social/<%= userId %>">👥 Social</a>
      </li>
      <li class="nav-item">
          <a class="nav-link" href="/timeoff/<%= userId %>">⏰ Time Off</a>
      </li>
      <li class="nav-item">
          <a class="nav-link" href="/body/<%= userId %>">💪 Body</a>
      </li>
      <li class="nav-item">
          <a class="nav-link" href="/auth/logout">🚪 Logout</a>
      </li>
      </ul>
    </div>
  </div>

  <div class="container mt-4">
    <h2 class="text-center mb-4">💳 Subscription Management</h2>

    <!-- Subscription Info -->
    <div class="card shadow-sm p-4 mb-4">
      <% if (subscriptionInfo.hasSubscription) { %>
        <h4 class="text-success mb-3">✅ You are a Premium Member</h4>
        <p>Thank you for supporting the app! You have access to all premium features.</p>
        
        <% if (subscriptionInfo.cancelAtPeriodEnd) { %>
          <div class="alert alert-warning" role="alert">
            <i class="fas fa-exclamation-triangle me-2"></i>
            <strong>Subscription Cancelled:</strong> Your subscription will end on 
            <%= new Date(subscriptionInfo.currentPeriodEnd).toLocaleDateString() %>. 
            You can reactivate anytime before then.
          </div>
        <% } else if (subscriptionInfo.currentPeriodEnd) { %>
          <p class="text-muted">Your subscription is active until: <%= new Date(subscriptionInfo.currentPeriodEnd).toLocaleDateString() %></p>
        <% } %>
        
        <% if (!subscriptionInfo.cancelAtPeriodEnd) { %>
          <button id="cancelSubscriptionBtn" class="btn btn-danger">
            <i class="fas fa-times me-2"></i>Cancel Subscription
          </button>
        <% } else { %>
          <button id="reactivateSubscriptionBtn" class="btn btn-success">
            <i class="fas fa-play me-2"></i>Reactivate Subscription
          </button>
        <% } %>
      <% } else { %>
        <h4 class="text-primary mb-3">🔓 You are on the Free Plan</h4>
        <p>Upgrade to premium to unlock extra features and support development.</p>
        
        <% if (typeof redirected !== 'undefined' && redirected) { %>
          <div class="alert alert-info" role="alert">
            <i class="fas fa-info-circle me-2"></i>
            <strong>Premium Feature Required:</strong> You need a premium subscription to access this feature. 
            Upgrade now to unlock all modules!
          </div>
        <% } %>
        
        <button id="subscribeBtn" class="btn btn-success w-100">
          <i class="fas fa-credit-card me-2"></i>Subscribe via Stripe
        </button>
      <% } %>
    </div>

    <!-- Premium Features -->
    <div class="card shadow-sm p-4 mb-4">
      <h4 class="text-info mb-3">🌟 Premium Features</h4>
      <ul class="list-group list-group-flush">
        <li class="list-group-item">✔️ Unlimited Tracking</li>
        <li class="list-group-item">✔️ Priority Support</li>
        <li class="list-group-item">✔️ Early Access to New Modules</li>
        <li class="list-group-item">✔️ Advanced Statistics and Reports</li>
        <li class="list-group-item">✔️ Access to All App Modules</li>
        <li class="list-group-item">✔️ Export Data</li>
      </ul>
    </div>

    <!-- Free Plan Limitations -->
    <div class="card shadow-sm p-4 mb-4">
      <h4 class="text-warning mb-3">⚠️ Free Plan Limitations</h4>
      <ul class="list-group list-group-flush">
        <li class="list-group-item">❌ Access only to Home Dashboard</li>
        <li class="list-group-item">❌ Limited tracking features</li>
        <li class="list-group-item">❌ No advanced analytics</li>
        <li class="list-group-item">❌ No data export</li>
      </ul>
    </div>
  </div>

  <script>
    // Pass subscription info to frontend JavaScript
    const subscriptionInfo = <%- JSON.stringify(subscriptionInfo) %>;
  </script>
  <script src="/frontendJS/subscribeFE.js"></script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
