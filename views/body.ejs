<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Workout Tracker</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <header class="bg-dark text-white p-3">
    <div class="container d-flex justify-content-between align-items-center">
      <div class="profile d-flex align-items-center">
        <!-- <img src="profile.jpg" alt="Profile Picture"> -->
        <span class="ms-2"><%= username %></span>
      </div>
      <button class="btn btn-outline-light" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar">
        ☰
      </button>
    </div>
  </header>

  <!-- Offcanvas Menu -->
  <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasNavbar">
    <div class="offcanvas-header">
      <h5 class="offcanvas-title">Menu</h5>
      <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas"></button>
    </div>
    <div class="offcanvas-body">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="#">Dashboard</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Settings</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Logout</a>
        </li>
      </ul>
    </div>
  </div>

  <div class="row g-4">
    <!-- Workouts Calendar -->
    <div class="col-md-6">
        <div class="workouts border p-4 rounded shadow">
            <h2 class="mb-3">Workouts Calendar</h2>
            <table class="table table-bordered text-center">
                <thead>
                    <tr>
                        <th>Sun</th>
                        <th>Mon</th>
                        <th>Tue</th>
                        <th>Wed</th>
                        <th>Thu</th>
                        <th>Fri</th>
                        <th>Sat</th>
                    </tr>
                </thead>
                <tbody>
                    <% 
                        const currentDate = new Date(); // Current date
                        const year = currentDate.getFullYear();
                        const month = currentDate.getMonth(); // 0-based (0 = January)
                        const firstDay = new Date(year, month, 1).getDay(); // Day the month starts on
                        const daysInMonth = new Date(year, month + 1, 0).getDate(); // Total days in the month
                        let date = 1; // Start counting dates for the month
                    %>

                    <% for (let week = 0; week < 6; week++) { // Render up to 6 weeks (rows) %>
                        <tr>
                            <% for (let day = 0; day < 7; day++) { %>
                                <% if (week === 0 && day < firstDay) { %>
                                    <!-- Empty cells before the start of the month -->
                                    <td></td>
                                <% } else if (date > daysInMonth) { %>
                                    <!-- Empty cells after the end of the month -->
                                    <td></td>
                                <% } else { %>
                                    <!-- Populate calendar with dates -->
                                    <td>
                                      <% 
                                        // Generate date string in 'YYYY-MM-DD' format
                                        const dateString = `${year}-${(month + 1).toString().padStart(2, '0')}-${date.toString().padStart(2, '0')}`;
                                        
                                        // Debug: Output the dateString and the list of completedWorkouts
                                        console.log("Generated dateString:", dateString);
                                        console.log("Formatted Completed Workouts:", completedWorkouts);
                                    
                                        // Find the workout in completedWorkouts for this date
                                        const workout = completedWorkouts.find(w => w.date === dateString);
                                    
                                        // Debug: Log the found workout
                                        console.log("Matching workout:", workout);
                                    
                                        // Check if the workout is marked as completed
                                        const isChecked = workout && workout.is_completed ? 'checked' : '';
                                      %>
                                      <div class="form-check">
                                        <input 
                                          class="form-check-input workout-checkbox" 
                                          type="checkbox" 
                                          id="day-<%= date %>" 
                                          data-workout-id="<%= workout ? workout.workout_id : '' %>" 
                                          data-date="<%= dateString %>" 
                                          <%= isChecked %> />
                                        <label class="form-check-label" for="day-<%= date %>">
                                          <%= date %>
                                        </label>
                                      </div>
                                    </td>
                                    
                                  
                                    <% date++; %>
                                <% } %>
                            <% } %>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>

      

      <!-- Workout Tracker -->
      <div class="col-md-6">
        <div class="workout-tracker border p-4 rounded shadow">
          <h2 class="mb-3">Workout Tracker</h2>
          <form action="/workouts" method="POST">
            <div class="mb-3">
              <label for="exerciseName" class="form-label">Exercise Name</label>
              <input type="text" id="exerciseName" name="exerciseName" class="form-control" placeholder="E.g., Push-ups">
            </div>
            <div class="mb-3">
              <label for="sets" class="form-label">Sets</label>
              <input type="number" id="sets" name="sets" class="form-control" placeholder="E.g., 3">
            </div>
            <div class="mb-3">
              <label for="reps" class="form-label">Reps</label>
              <input type="number" id="reps" name="reps" class="form-control" placeholder="E.g., 12">
            </div>
            <button type="submit" class="btn btn-primary w-100">Add Exercise</button>
          </form>

          <!-- List of Exercises -->
          <div class="mt-4">
            <h3>Exercises</h3>
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>Exercise</th>
                  <th>Sets</th>
                  <th>Reps</th>
                </tr>
              </thead>
              <tbody>
                <% if (exercises && exercises.length > 0) { %>
                  <% exercises.forEach((exercise) => { %>
                  <tr>
                    <td><%= exercise.name %></td>
                    <td><%= exercise.sets %></td>
                    <td><%= exercise.reps %></td>
                  </tr>
                  <% }) %>
                <% } else { %>
                <tr>
                  <td colspan="3" class="text-center">No exercises added yet</td>
                </tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/frontendJS/bodyFE.js"></script>
</body>
</html>
